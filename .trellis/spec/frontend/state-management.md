# 状态管理

## 默认策略

**无全局状态库。** 订阅页仅保留页面级局部变量（筛选勾选、日历视图/游标、`news.json` 内存缓存）。

## 允许的状态

- 筛选复选框与「已复制」提示
- 日历视图（月/周/日）与当前游标日期
- 从 `/data/news.json` 加载到内存的列表（只读，不当作长期缓存源）

## 禁止

- Redux / Zustand / 大型服务端状态方案
- 把抓取快照缓存在浏览器 LocalStorage 充当权威数据源
- 为日历交互引入前端框架状态机
